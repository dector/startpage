version: "3"

includes:
  deploy: assets/deploy.Taskfile.yml

tasks:
  default:
    cmds:
      - task --list-all

  deploy:
    cmds:
      - ror build
      - task: deploy:config
      - task: deploy:service
    silent: true
