version: "3"

tasks:
  dev:
    cmds:
      - DEV=1 air
    silent: true
  build:
    cmds:
      - go build -o bin/sp.exe cmd/main.go
    silent: true
  deploy:
    deps:
      - build
    cmds:
      - cp bin/sp.exe /opt/startpage/bin/sp.exe
      - systemctl restart startpage
    silent: true
